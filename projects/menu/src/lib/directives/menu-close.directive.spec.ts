import { MenuCloseDirective } from './menu-close.directive';

describe('MenuCloseDirective', () => {
  it('should create an instance', () => {
    // const directive = new MenuCloseDirective();
    // expect(directive).toBeTruthy();
  });
});
